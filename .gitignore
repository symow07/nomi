node_modules/
dist/
.env
.env.*
!.env.example
*.log
.DS_Store

# Per-machine Claude Code state. Ignored by ~/.config/git/ignore on the machine
# this was written on, which protects nobody else's checkout or CI.
.claude/settings.local.json

# G17b — tools/screenshots.mjs output, for review by eye; never committed.
/screenshots/

# The pre-pilot walkthrough's rendered pages and report.
/pre-pilot/
